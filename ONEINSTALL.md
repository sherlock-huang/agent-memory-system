# One-Line Installer
# 在新机器上运行这一条命令即可安装并测试

# Windows PowerShell
# (打开 PowerShell，粘贴下面这一行)
powershell -Command "Invoke-WebRequest -Uri 'https://你的仓库地址/install.ps1' -OutFile 'install.ps1'; .\install.ps1"

# Linux/macOS
# curl -sL https://你的仓库地址/install.sh | bash
